name: flutter_monocypher
description: "A new Dart FFI package project."
version: 0.0.1

environment:
  sdk: ^3.12.0

dependencies:
  code_assets: ^1.0.0
  ffi: ^2.2.0
  hooks: ^1.0.1
  logging: ^1.3.0
  native_toolchain_c: ^0.17.4

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0

# clang --target=wasm32 -O3 -nostdlib -Wl,--no-entry -Wl,--export-all -o assets/monocypher.wasm src/monocypher_wasm.c

flutter:
  assets:
    - path: assets/monocypher.wasm
      platforms:
        - web
